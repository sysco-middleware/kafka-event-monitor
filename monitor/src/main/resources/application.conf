event-monitor {
  kafka {
    bootstrap-servers = "localhost:29092"
    bootstrap-servers = ${?KAFKA_BOOTSTRAP_SERVERS}
  }

  topics = [
    {
      name = "topic1"
      status = "first"
      id = {
        source = KEY
        key = string
      }
    }
  ]
}